#include <stdio.h>

int main() {
    int length, breadth;
    
    scanf("%d", &length);
    scanf("%d", &breadth);
    
    printf("%d\n", 2 * (length + breadth));
    printf("%d\n", length * breadth);
    
    return 0;
}
